import { Text, View } from "react-native";
import theme from "../theme";

export default function ProfileScreen() {
    return (
        <View>
            <Text style={theme.fonts.h1}>Profile</Text>
        </View>
    );
}