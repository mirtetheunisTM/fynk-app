import { Text, View } from "react-native";
import theme from "../theme";

export default function FriendsScreen() {
    return (
        <View>
            <Text style={theme.fonts.h1}>Friends</Text>
        </View>
    );
}