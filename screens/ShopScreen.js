import { Text, View } from "react-native";
import theme from "../theme";

export default function ShopScreen() {
    return (
        <View>
            <Text style={theme.fonts.h1}>Shop</Text>
        </View>
    );
}